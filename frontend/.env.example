# API base URL for the LMS backend (no trailing slash). Do not add /api.
# Dev: http://localhost:4000
# Production: https://learning-managment-platform-backend.onrender.com
VITE_API_BASE_URL=http://localhost:4000

# Set to true for local-only mode: no backend, easy sign-in and navigation.
# VITE_USE_MOCK=true
